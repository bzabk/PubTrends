version: '3'

services:
  streamlit-app:
    build: .
    ports:
      - "8501:8501"
    depends_on:
      - redis-db
    environment:
      - REDIS_HOST=redis-db
  redis-db:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - ./src/ApiClient/RedisData:/data



